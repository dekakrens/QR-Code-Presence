<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.user.myschool">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="QR Mobilence"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LihatDaftar" />
        <activity android:name=".Profile" />
        <activity android:name=".Guru" />
        <activity android:name=".SiswaLogin" />
        <activity android:name=".SiswaSignup" />
        <activity android:name=".GuruLogin" />
        <activity android:name=".GuruSignup" />
    </application>

</manifest>